import styled from 'styled-components'

const Home = styled.div`
display: flex;
height: 100%;
align-items: center;
justify-content: center;
`

export default () => (
  <Home>
    Click a report on the left.
  </Home>
)
